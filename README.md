# Iris Flower Classification App

This is a machine learning web application built using **Streamlit** that predicts the species of an Iris flower based on its sepal and petal measurements.

## How to Run the App
1. Install the required libraries:
   ```bash
   pip install streamlit pandas scikit-learn